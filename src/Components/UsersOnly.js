import React from "react";

const UsersOnly = () => {
    return (
        <div>
            <h1>Users Only</h1>
            <p>Only authenticated users can view this page.</p>
        </div>
    );
};

export default UsersOnly;
